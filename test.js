const getGreeting = require('./project');

test('can create a basic greeting', () => {
  expect(getGreeting('Sam')).toBe("Hi Sam");
});
return
const addUser = require('./project');
test('can add a new user',() => {
  expect(addUser('kyson')).toBe("Buenos dias kyson");
});
return
